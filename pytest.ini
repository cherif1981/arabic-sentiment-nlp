[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    --color=yes
    -ra
markers =
    slow: اختبارات بطيئة
    requires_model: تحتاج نموذجاً مدرباً
    requires_api: تحتاج API يعمل